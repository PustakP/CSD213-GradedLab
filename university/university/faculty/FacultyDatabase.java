package university.faculty;

import university.UniversityMember;
import university.UniversityMemberDatabase;

public class FacultyDatabase extends UniversityMemberDatabase {
    public FacultyDatabase(int capacity) {
        super(capacity);
    }
}

