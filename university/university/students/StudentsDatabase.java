package university.students;

import university.UniversityMember;
import university.UniversityMemberDatabase;

public class StudentsDatabase extends UniversityMemberDatabase {
    public StudentsDatabase(int capacity) {
        super(capacity);
    }
}

